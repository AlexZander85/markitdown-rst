name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.edition }} for ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Full edition (GUI + OCR + Preview) ──
          # OCR uses tesseract CLI subprocess — no compile-time C deps needed
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            edition: full
            features: "--features ocr,preview,simd"
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            edition: full
            features: "--features ocr,preview,simd"
          - os: macos-latest
            target: aarch64-apple-darwin
            edition: full
            features: "--features ocr,preview,simd"
          # ── Light edition (GUI, no OCR, no Preview, with SIMD) ──
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            edition: light
            features: "--no-default-features --features gui,simd"
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            edition: light
            features: "--no-default-features --features gui,simd"
          - os: macos-latest
            target: aarch64-apple-darwin
            edition: light
            features: "--no-default-features --features gui,simd"
          # ── CLI-only edition (no GUI, with OCR + SIMD) ──
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            edition: cli-only
            features: "--no-default-features --features cli-only,ocr,simd"
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            edition: cli-only
            features: "--no-default-features --features cli-only,ocr,simd"
          - os: macos-latest
            target: aarch64-apple-darwin
            edition: cli-only
            features: "--no-default-features --features cli-only,ocr,simd"

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb1-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev \
            libx11-dev libx11-xcb-dev libxrandr-dev libxi-dev libxcursor-dev \
            libxdamage-dev libxfixes-dev libxinerama-dev libwayland-dev \
            libgbm-dev libegl-dev libspeechd-dev

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.os }}-${{ matrix.edition }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ matrix.os }}-${{ matrix.edition }}-cargo-

      # ── Build ──
      - name: Build GUI binary (Full / Light)
        if: matrix.edition != 'cli-only'
        run: cargo build --release --bin mdrust --target ${{ matrix.target }} ${{ matrix.features }}

      - name: Build CLI binary (CLI-only)
        if: matrix.edition == 'cli-only'
        run: cargo build --release --bin mdrust-cli --target ${{ matrix.target }} ${{ matrix.features }}

      # ── Prepare artifacts ──
      # GUI:  mdrust-{edition}-{os}-x64[.exe]
      # CLI:  mdrust-cli-{os}-x64[.exe]  (no edition suffix — one CLI per OS)
      - name: Prepare artifacts (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $edition = "${{ matrix.edition }}"
          $target = "${{ matrix.target }}"

          if ($edition -ne "cli-only") {
            $src = "target/$target/release/mdrust.exe"
            $dst = "mdrust-${edition}-windows-x64.exe"
            Copy-Item $src $dst
            Write-Host "GUI artifact: $dst ($('{0:N0}' -f (Get-Item $dst).Length) bytes)"
          }

          if ($edition -eq "cli-only") {
            $src = "target/$target/release/mdrust-cli.exe"
            $dst = "mdrust-cli-windows-x64.exe"
            Copy-Item $src $dst
            Write-Host "CLI artifact: $dst ($('{0:N0}' -f (Get-Item $dst).Length) bytes)"
          }

      - name: Prepare artifacts (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          EDITION="${{ matrix.edition }}"
          TARGET="${{ matrix.target }}"

          if [ "$EDITION" != "cli-only" ]; then
            cp "target/$TARGET/release/mdrust" "mdrust-${EDITION}-linux-x64"
            chmod +x "mdrust-${EDITION}-linux-x64"
            echo "GUI artifact: mdrust-${EDITION}-linux-x64 ($(stat -c%s mdrust-${EDITION}-linux-x64) bytes)"
          fi

          if [ "$EDITION" = "cli-only" ]; then
            cp "target/$TARGET/release/mdrust-cli" "mdrust-cli-linux-x64"
            chmod +x "mdrust-cli-linux-x64"
            echo "CLI artifact: mdrust-cli-linux-x64 ($(stat -c%s mdrust-cli-linux-x64) bytes)"
          fi

      - name: Prepare artifacts (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          EDITION="${{ matrix.edition }}"
          TARGET="${{ matrix.target }}"

          if [ "$EDITION" != "cli-only" ]; then
            cp "target/$TARGET/release/mdrust" "mdrust-${EDITION}-macos-arm64"
            chmod +x "mdrust-${EDITION}-macos-arm64"
            echo "GUI artifact: mdrust-${EDITION}-macos-arm64 ($(stat -f%z mdrust-${EDITION}-macos-arm64) bytes)"
          fi

          if [ "$EDITION" = "cli-only" ]; then
            cp "target/$TARGET/release/mdrust-cli" "mdrust-cli-macos-arm64"
            chmod +x "mdrust-cli-macos-arm64"
            echo "CLI artifact: mdrust-cli-macos-arm64 ($(stat -f%z mdrust-cli-macos-arm64) bytes)"
          fi

      # ── Upload ──
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdrust-${{ matrix.edition }}-${{ matrix.target }}
          path: |
            mdrust-*
            mdrust-cli-*

  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: List downloaded artifacts
        run: find artifacts/ -type f | sort

      - name: Create release archives
        run: |
          mkdir -p release-assets
          cd artifacts

          # Create .tar.gz for Linux binaries
          for f in mdrust-*-linux-x64 mdrust-cli-linux-x64; do
            if [ -f "$f" ]; then
              tar czf "../release-assets/${f}.tar.gz" "$f"
              echo "Packaged: ${f}.tar.gz"
            fi
          done

          # Copy Windows .exe as-is
          for f in mdrust-*-windows-x64.exe mdrust-cli-windows-x64.exe; do
            if [ -f "$f" ]; then
              cp "$f" "../release-assets/$f"
              echo "Copied: $f"
            fi
          done

          # Create .tar.gz for macOS binaries
          for f in mdrust-*-macos-arm64 mdrust-cli-macos-arm64; do
            if [ -f "$f" ]; then
              tar czf "../release-assets/${f}.tar.gz" "$f"
              echo "Packaged: ${f}.tar.gz"
            fi
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false
          body_path: RELEASE_NOTES.md
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
